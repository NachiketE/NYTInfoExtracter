<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Top Stories - <%= section %></title>
</head>
<body>
    <h1>Top Stories - <%= section %></h1>
    <ul>
        <% articles.forEach(article => { %>
            <li>
                <h2><%= article.title %></h2>
                <p><%= article.abstract %></p>
                <p><a href="<%= article.url %>">Read more</a></p>
            </li>
        <% }); %>
    </ul>
</body>
</html>
